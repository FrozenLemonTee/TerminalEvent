name = "FrozenLemonTee/TerminalEvent"

version = "0.1.0"

readme = "README.md"

repository = "https://github.com/FrozenLemonTee/TerminalEvent"

license = "Apache-2.0"

keywords = [ "tui", "terminal", "event", "ffi" ]

description = "C++ terminal event backend with a stable C FFI surface"

preferred_target = "native"
